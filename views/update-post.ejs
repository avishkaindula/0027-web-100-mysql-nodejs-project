<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: 'Update post' }) %>
    <link rel="stylesheet" href="/styles/forms.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <main>
      <h1>Update post</h1>
      <form action="/posts/<%= post.id %>/edit" method="POST">
        <!-- In order to send user generated data into the database, we need to -->
        <!-- use the POST method in here. -->
        <!-- We can send the request to the same path we used to send the load the edit page. -->
        <!-- But now it's reached with help of a post request which is therefor triggers a different route. -->
        <!-- (because GET and POST requests don't trigger the same route.) -->
        <!-- now we can create a router.post() route in the blog,js -->
        <div class="form-control">
          <label for="title">Title</label>
          <input
            type="text"
            id="title"
            value="<%= post.title %>"
            name="title"
            required
          />
          <!-- "value" attribute will be used to pre-populate the input field -->
          <!-- from the already inserted post data on the database -->
          <!-- We can use the "post" key we created on the posts/:id/edit route on blog.js to -->
          <!-- populate the values. -->
        </div>
        <div class="form-control">
          <label for="summary">Summary</label>
          <input
            type="text"
            id="summary"
            value="<%= post.summery %>"
            name="summary"
            required
            maxlength="255"
          />
          <!-- I've misspelled summary as "summery" on the blog sql database -->
          <!-- Therefor, I also need to misspell it in here. -->
        </div>
        <div class="form-control">
          <label for="content">Post Content</label>
          <textarea id="content" name="content" required rows="5">
<%= post.body %></textarea
          >
          <!-- We shouldn't add a line break before post.body -->
          <!-- If we do that it will also be a part of this text area as a result -->
          <!-- of the white-space: pre-wrap property we created on the posts.css file.  -->
        </div>
        <button class="btn">Update Post</button>
      </form>
    </main>
  </body>
</html>
