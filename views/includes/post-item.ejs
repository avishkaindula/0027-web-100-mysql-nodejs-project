<article class="post-item">
  <h2><%= post.title %></h2>
  <p class="post-item-author">By <%= post.author_name %></p>
  <!-- We've chosen author_name as the alias in the query on the blog.js. so we need to use that name in here. -->
  <!-- SELECT posts.*, authors.name AS author_name -->
  <p><%= post.summery %></p>
  <!-- I've misspelled summary as "summery" in the mysql workbench. -->
  <!-- Therefor, we need to misspell it in here to. -->
  <div class="post-actions">
    <form action="/posts/<%= post.id %>/delete" method="POST">
      <!-- post request is sent to this path on the action attribute when this button is clicked. -->
      <!-- now we can create this path in the blog.js -->
      <button class="btn btn-alt">Delete Post</button>
    </form>

    <a href="/posts/<%= post.id %>/edit">Edit Post</a>
    <!-- This is the route we created on the blog.js for update-post.ejs -->

    <a class="btn" href="/posts/<%= post.id %> ">View Post</a>
    <!-- We need to create different urls for different posts. -->
    <!-- We can achieve this by using the already created id of the post -->
    <!-- So we don't need to use the uuid package in here. -->
  </div>
</article>
